import 'express-async-errors';
import { Router } from 'express';
import { RequestHandler } from 'express';

const authRouter = Router();

authRouter.get('/', (async (_req, _res) => {}) as RequestHandler);
